### Config 동적모듈로 구성하는 이유
```
개인적으로 느끼는것은 app.module코드가 깔끔하고 이로인해 유지보수가 쉬워진다.
예를들어 TypeORM설정하는 곳에서 따로 동적모듈로한다면 Injectable을 사용해 프로바이더로 만들고 이로인해
ConfigService를 사용하고 이를 통해서 DB설정을 한다.
그런데 이 프로바이더를 Config설정할때 동적모듈로 useClass를 통해 프로바이더로 지정하여 사용이 가능하여 편리하다.
```
