### 세팅 CLI 자주사용하는거
```ts
pnpm add -D eslint
pnpm add -D prettier
pnpm add eslint-config-prettier
pnpm add eslint-plugin-prettier

pnpm add @nestjs/config cross-env
pnpm add typeorm @nestjs/typeorm mysql2
pnpm add class-validator class-transformer
```
